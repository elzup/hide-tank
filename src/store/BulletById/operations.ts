import { ThunkAction } from '../../types'

export function shotBullet(): ThunkAction {
  return async (dispatch, getState) => {
    // player 取り出し
    // 残弾数チェック
    // Bullet obj
    // id を振る
    // await receiveBullet
    // player.bullets に追加
  }
}
